document.querySelector('.start-btn').addEventListener('click', createname);

function createname() {
    var name = document.querySelector('.user-name'),
    usernanme = input.value;

    document.querySelector('.username').innerHTML = name;
}
